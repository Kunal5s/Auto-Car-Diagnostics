
"use client";

// This component is currently disabled as all external AI features have been removed.
// It can be re-enabled in the future if a different text-generation service is chosen.
export function ArticleSummarizer({ articleContent }: { articleContent: string }) {
  return null;
}
