
"use client";

// This component is currently disabled as all external AI features have been removed.
// It can be re-enabled in the future if a different service is chosen.
export function ImageGenerator() {
  return null;
}
